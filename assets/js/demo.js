document.addEventListener('DOMContentLoaded', function () {
    if (document.body && document.body.classList.contains('rex-page-builder-demo')) {
        window.scrollTo({
            top: 0,
            left: 0,
            behavior: 'auto',
        });
    }
});
